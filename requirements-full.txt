# ===========================================================
# وابستگی‌های کامل برای اجرای llm_script_final.py
# (نسخهٔ سنگین: RAG + LLM + OCR + Gradio)
# نصب: pip install -r requirements-full.txt
#
# معماری: LLM (qwen2.5:7b) روی سرویس جدا Ollama با GPU اجرا می‌شود؛ این کانتینر فقط
# embedder را روی CPU اجرا می‌کند. پس اینجا torch نسخهٔ CPU کافی است و نیازی به
# torch مخصوص GPU/CUDA (و دردسر سازگاری Blackwell) نیست.
# ⚠️ torch به‌صورت CPU در Dockerfile.full جداگانه نصب می‌شود (index رسمی PyTorch CPU).
# ===========================================================

# ----- API / وب‌سرور -----
fastapi
uvicorn[standard]
python-multipart          # لازم برای UploadFile در Endpoint /upload
websockets

# ----- ML / هوش مصنوعی -----
ollama                    # کلاینت LLM؛ به سرویس Ollama (GPU) وصل می‌شود
transformers              # وابستگی sentence-transformers (روی CPU)
sentence-transformers     # embedder فارسی (روی CPU اجرا می‌شود)
faiss-cpu

# ----- OCR و پردازش فایل -----
pytesseract
pdf2image
pillow
pdfplumber
arabic-reshaper
python-bidi
opencv-python-headless
numpy

# ----- رابط گرافیکی -----
gradio

# ----- دیتابیس -----
mysql-connector-python
